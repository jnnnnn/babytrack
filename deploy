#!/bin/bash
set -euo pipefail

cd "$(dirname "$0")"

echo "=== Verify first ==="
./verify

echo "=== Deploy to fly.io ==="
cd server
fly deploy --ha=false

echo ""
echo "✓ Deployed"
echo "View logs: fly logs -a babytrack"

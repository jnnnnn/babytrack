#!/bin/bash
set -euo pipefail

cd "$(dirname "$0")"

echo "=== Format ==="
gofmt -w server/

echo "=== Lint ==="
# TODO: golangci-lint run ./server/...

echo "=== Test ==="
(cd server && go test -cover ./...)

echo "=== Frontend ==="
# TODO: run playwright tests against frontend / local backend

echo ""
echo "✓ All checks passed"
